{
  "name": "hop-email",
  "version": "0.9.0",
  "description": "Email server for Hop!",
  "author": "Robert Saunders and James Simpson",
  "scripts": {
    "dev": "npm run clean && mjml ./templates/*.mjml -o ./dist/",
    "build": "npm run clean && mjml --min ./templates/*.mjml -o ./dist/",
    "clean": "rimraf ./dist"
  },
  "dependencies": {
    "@sendgrid/mail": "^6.2.1"
  },
  "devDependencies": {
    "mustache": "^2.3.0",
    "mjml": "^4.0.5",
    "rimraf": "^2.6.2"
  }
}
